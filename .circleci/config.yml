version: 2.1

jobs:
  build:
    docker:
      - image: node:18
    steps:
      - checkout
      - run: npm install
      - run: node index.js 

workflows:
  version: 2
  build_and_test:
    jobs:
      - build